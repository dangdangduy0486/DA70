import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Loading = () => {
  return (
    <View>
      <Text style={{ backgroundColor: "black", height: 100, width: "100%" }}>
        Loading
      </Text>
    </View>
  );
};

export default Loading;

const styles = StyleSheet.create({});
